export type EasybankItem = {
    icon: string;
    title: string;
    description: string;
};

export type ArticleItem = {
    image: string;
    autor: string;
    title: string;
    description: string;
};
